// ? asynchronus

// ? synchronus => existing or occuring at the same time.
// ? asynchronus => not existing or occuring at the same time.

// ? 1.node => single threaded
// ? 2.non-blocking => does not block the code execution
// ? 3.real world scernario of code blocking =>
// ? for loop for billion times
// ? image processing
// ? Api request
// ? db operation

// U => ultra power saving
// k => unlocked
// H => High performance

// console.log("Hi");

// setTimeout(() => {
//   console.log("Timeout");
// }, 20000);

// console.log("Hello");
